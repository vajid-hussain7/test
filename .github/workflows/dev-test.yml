
name: test-demo-workflow
run-name: ${{github.actor}} testing our github action
on: 
  push: 
    branches: 
      - main

jobs: 
  first-job:
    runs-on: ubuntu-latest
    environment: develop
    steps:
      - name: pwd run
        shell: bash
        run: pwd
      
      # - name: print workspace
      #   shell: bash
      #   run: go run main.go

      - name: environment secret
        shell: bash
        run: echo ${{secrets.MYJWT}}

      # - name:  install
      #   shell: bash
      #   run: sudo apt install tree

      # - name:  tree
      #   shell: bash
      #   run: treeing